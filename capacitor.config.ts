import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.angular',
  appName: 'tabsTestAngular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
